// Dart annotations require compile-time constants that can be fully
// verified by the analyzer, so typeIds must be declared as const values
// rather than expressions or runtime-generated values.
abstract class HiveTypeIds {
  static const int userProgressModel = 1;
}
